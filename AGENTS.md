# Agent Notes

- Keep this file instruction-only.
- Do not repeat project listings, repository maps, or other content already covered in `README.md` or the app READMEs.
- Keep runnable example code under `apps/<name>/project/`.
- Keep catalog metadata, bootstrap configs, and `example.json` at `apps/<name>/`.
- When changing an example, update the catalog README and the matching `project/README.md` only if the change affects that layer.
