REVISIUM_URL=revisium://localhost:9222/admin/dictionary/master
# Local-only connection. For a standalone local instance this value is the only
# required field because auth is disabled locally by default.
